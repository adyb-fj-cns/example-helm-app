bluegreen:
    replicaCount: 5

    image:
        repository: nginx
        tag: 1.15.5

    service:
        type: NodePort

    ingress:
        enabled: true
        annotations:
            kubernetes.io/ingress.class: nginx
            # kubernetes.io/tls-acme: "true"
        hosts:
        - host: example-help-app.kubernetes.local
          paths: []
    